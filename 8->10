#include <stdio.h>

int main(void)
{
    int oct, digit;
    int decimal = 0;
    int base = 1;

    scanf("%d", &oct);

    while (oct > 0) {
        digit = oct % 10;
        decimal += digit * base;
        base *= 8;
        oct /= 10;
    }

    printf("%d\n", decimal);
    return 0;
}
